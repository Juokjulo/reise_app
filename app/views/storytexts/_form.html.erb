<%= form_for(storytext) do |f| %>
  <% if storytext.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(storytext.errors.count, "error") %> prohibited this storytext from being saved:</h2>

      <ul>
      <% storytext.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :storypart %>
    <%= f.text_area :storypart %>
  </div>

  <div class="actions">
    <%= f.submit%>
  </div>
<% end %>
