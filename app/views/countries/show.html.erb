<%= render 'layouts/container' %>

<p>
  <strong>Name:</strong>
  <%= @country.name %>
</p>

<p>
  <strong>Titleimage:</strong>
  <%= image_tag(@country.picture.image_url, :width => 60) if @country.picture.image.present? %>
</p>

<p>
  <strong>Shortdescription:</strong>
  <%= @country.shortDescription %>
</p>

     
<%= link_to 'Edit', edit_country_path(@country) %> |
<%= link_to 'Back', countries_path %>

<%= render 'layouts/container_end' %>