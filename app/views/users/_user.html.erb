<%if current_user.try(:admin?)%>
	<td><%= link_to user.name, user %></td>
	<td><%= link_to user.email, user %></td>
	<td>Newsletter: <%= "Ja" if user.newsletter_abo %><%= "Nein" if ! user.newsletter_abo %></td>
	<td>Admin: <%= "Ja" if user.admin %><%= "Nein" if ! user.admin %></td>
	<% if current_user.try(:admin?) %>
           <td>   <%= link_to 'reset', reset_password_path(user), :class => "btn btn-danger", :role => "button"  %></td>
     <% end %> 
<%end%>